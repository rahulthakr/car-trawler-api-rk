(function( $ ) {
    $(function() {
         
        // Add Color Picker to all inputs that have 'color-field' class
        $( '.cartrawler-color-picker' ).wpColorPicker();
         
    });
})( jQuery );